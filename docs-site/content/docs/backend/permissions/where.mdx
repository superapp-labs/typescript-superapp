---
title: Where Clauses
description: Inject WHERE clauses to restrict which rows users can access, from simple equality to FK traversal.
icon: ListFilter
---

Where clauses add SQL WHERE conditions to every query, ensuring users only see and modify rows they have access to. They live inside operation blocks — `select`, `update`, and `delete`.

```typescript
permissions: {
  view_own_orders: {
    table: 'main.orders',
    roles: ['customer'],
    select: {
      columns: ['id', 'amount', 'status', 'customer_id', 'created_at'],
      where: { customer_id: { $eq: '$user.id' } },
    },
  },
}
```

**Generated SQL:**

```sql
SELECT id, amount, status, customer_id, created_at
FROM main.orders
WHERE customer_id = 'usr_123'  -- injected by where clause
```

## Simple Where Clauses

Direct column comparisons:

```typescript
// Exact match
where: { customer_id: { $eq: '$user.id' } }
// → WHERE customer_id = 'usr_123'

// Multiple values
where: { status: { $in: ['active', 'pending'] } }
// → WHERE status IN ('active', 'pending')

// Range
where: { amount: { $gte: 0, $lte: 50_000 } }
// → WHERE amount >= 0 AND amount <= 50000

// Multiple conditions (AND)
where: {
  organization_id: { $eq: '$user.current_org_id' },
  status: { $ne: 'deleted' },
}
// → WHERE organization_id = 'org_456' AND status != 'deleted'
```

## Array Session Variables

When `$user.*` resolves to an array, use `$in`:

```typescript
where: { organization_id: { $in: '$user.org_ids' } }
// If $user.org_ids = ['org_1', 'org_2', 'org_3']
// → WHERE organization_id IN ('org_1', 'org_2', 'org_3')
```

## Relationship Traversal

Filter through foreign key relationships by nesting objects. Each key is a related table, and the engine follows the FK path:

```typescript
select: {
  columns: '*',
  where: {
    organization: {
      members: {
        user_id: { $eq: '$user.id' },
      },
    },
  },
}
```

**Generated SQL:**

```sql
SELECT main.orders.*
FROM main.orders
WHERE main.orders.organization_id IN (
  SELECT id FROM main.organizations
  WHERE id IN (
    SELECT organization_id FROM main.members
    WHERE user_id = 'usr_123'
  )
)
```

### How Path Resolution Works

The engine resolves nested where objects by inspecting FK relationships in your schema:

```text
main.orders
  → organization (FK: orders.organization_id → organizations.id)
    → members (FK: members.organization_id → organizations.id)
      → user_id = $user.id
```

1. `organization` -- looks for an `organization_id` column on `main.orders` that references `main.organizations`
2. `members` -- looks for a relationship between `main.organizations` and `main.members`
3. `user_id: { $eq: '$user.id' }` -- the leaf condition applied to `main.members`

### Single Hop

Filter orders by their customer's status:

```typescript
select: {
  columns: '*',
  where: {
    customer: {
      status: { $eq: 'active' },
    },
  },
}
```

```sql
SELECT * FROM main.orders
WHERE customer_id IN (
  SELECT id FROM main.customers
  WHERE status = 'active'
)
```

### Two Hops with Role Check

Filter orders where the user is an admin or owner of the organization:

```typescript
select: {
  columns: '*',
  where: {
    organization: {
      members: {
        user_id: { $eq: '$user.id' },
        role: { $in: ['owner', 'admin'] },
      },
    },
  },
}
```

```sql
SELECT * FROM main.orders
WHERE organization_id IN (
  SELECT id FROM main.organizations
  WHERE id IN (
    SELECT organization_id FROM main.members
    WHERE user_id = 'usr_123'
      AND role IN ('owner', 'admin')
  )
)
```

### Combined Direct and FK Where Clauses

Mix direct column conditions with FK traversal:

```typescript
select: {
  columns: '*',
  where: {
    status: { $ne: 'deleted' },
    organization: {
      members: {
        user_id: { $eq: '$user.id' },
      },
    },
  },
}
```

```sql
SELECT * FROM main.orders
WHERE status != 'deleted'
  AND organization_id IN (
    SELECT id FROM main.organizations
    WHERE id IN (
      SELECT organization_id FROM main.members
      WHERE user_id = 'usr_123'
    )
  )
```

### Depth Limits

The `maxFilterDepth` setting controls how many FK hops are allowed:

```typescript
limits: {
  maxFilterDepth: 5,  // default
}
```

If a where clause exceeds this depth, the request returns `400 Bad Request`.

## Complex Where Clauses with Logical Operators

Combine conditions with `$or`, `$and`, and `$not`:

```typescript
select: {
  columns: '*',
  where: {
    $or: [
      { customer_id: { $eq: '$user.id' } },
      { organization_id: { $in: '$user.org_ids' } },
    ],
  },
}
// → WHERE (customer_id = 'usr_123' OR organization_id IN ('org_1', 'org_2'))
```

```typescript
select: {
  columns: '*',
  where: {
    $and: [
      { status: { $ne: 'deleted' } },
      {
        $or: [
          { customer_id: { $eq: '$user.id' } },
          { assigned_to: { $eq: '$user.id' } },
        ],
      },
    ],
  },
}
// → WHERE status != 'deleted' AND (customer_id = 'usr_123' OR assigned_to = 'usr_123')
```

## Where Clause Merging

When a user has multiple permissions for the same table, where clauses are merged with OR:

```typescript
// Permission 1: view_own_orders
select: { where: { customer_id: { $eq: '$user.id' } } }

// Permission 2: view_org_orders
select: { where: { organization_id: { $in: '$user.org_ids' } } }

// Merged result:
// → WHERE (customer_id = 'usr_123') OR (organization_id IN ('org_1', 'org_2'))
```

## Where Clauses on Update and Delete

Where clauses also work inside `update` and `delete` operation blocks to restrict which rows can be modified or removed:

```typescript
permissions: {
  edit_own_orders: {
    table: 'main.orders',
    roles: ['customer'],
    update: {
      columns: ['status', 'amount'],
      where: { customer_id: { $eq: '$user.id' } },
    },
  },
  delete_own_drafts: {
    table: 'main.orders',
    roles: ['customer'],
    delete: {
      where: {
        customer_id: { $eq: '$user.id' },
        status: { $eq: 'draft' },
      },
    },
  },
}
```

## All Where Operators

See [Permission Operators](/docs/backend/permissions/operators) for the complete list of comparison and logical operators.
